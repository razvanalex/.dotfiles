# alias advertised on the LAN
alias=Razvan PC
# where received files land
save_dir=~/Downloads
# optional 6-digit pin (empty = none)
pin=
# auto-accept inbound (false = manual per-request)
auto_accept=false
# tcp/udp port + multicast group
port=53317
group=224.0.0.167
